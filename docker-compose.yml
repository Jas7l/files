services:
  backend:
    build:
      context: ./backend-app
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend-app/storage:/app/storage
      - ./backend-app/files.db:/app/files.db
    restart: unless-stopped
